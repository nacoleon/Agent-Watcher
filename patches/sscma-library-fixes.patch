diff --git a/components/esp_lvgl_port/esp_lvgl_port.c b/components/esp_lvgl_port/esp_lvgl_port.c
index 0a70bda..592c82a 100644
--- a/components/esp_lvgl_port/esp_lvgl_port.c
+++ b/components/esp_lvgl_port/esp_lvgl_port.c
@@ -867,7 +867,14 @@ static void lvgl_port_flush_callback(lv_disp_drv_t *drv, const lv_area_t *area,
     const int offsety1 = area->y1;
     const int offsety2 = area->y2;
     // copy a buffer's content to a specific area of the display
-    esp_lcd_panel_draw_bitmap(disp_ctx->panel_handle, offsetx1, offsety1, offsetx2 + 1, offsety2 + 1, color_map);
+    esp_err_t flush_ret = esp_lcd_panel_draw_bitmap(disp_ctx->panel_handle, offsetx1, offsety1, offsetx2 + 1, offsety2 + 1, color_map);
+    if (flush_ret != ESP_OK) {
+        ESP_LOGE(TAG, "FLUSH ERR: ret=0x%x area=(%d,%d)-(%d,%d) — draw_bitmap failed after retry",
+            flush_ret, offsetx1, offsety1, offsetx2, offsety2);
+        // draw_bitmap already retried internally. If still failing, signal flush complete
+        // to prevent permanent LVGL mutex deadlock.
+        lv_disp_flush_ready(drv);
+    }
 }
 
 static void lvgl_port_update_callback(lv_disp_drv_t *drv)
diff --git a/components/rlottie/rlottie b/components/rlottie/rlottie
--- a/components/rlottie/rlottie
+++ b/components/rlottie/rlottie
@@ -1 +1 @@
-Subproject commit b13497a95b0f7b92d3ed0e45d6b1dbf93d13944f
+Subproject commit b13497a95b0f7b92d3ed0e45d6b1dbf93d13944f-dirty
diff --git a/components/sensecap-watcher/sensecap-watcher.c b/components/sensecap-watcher/sensecap-watcher.c
index c07f560..87c8437 100644
--- a/components/sensecap-watcher/sensecap-watcher.c
+++ b/components/sensecap-watcher/sensecap-watcher.c
@@ -762,7 +762,7 @@ static lv_indev_t *bsp_touch_indev_init(lv_disp_t *disp)
     const lvgl_port_touch_cfg_t touch = {
         .disp = disp,
         .handle = tp_handle,
-        .sensitivity = CONFIG_LVGL_INPUT_DEVICE_SENSITIVITY,
+        .sensitivity = 50,
     };
     return lvgl_port_add_touch(&touch);
 }
diff --git a/components/sscma_client/CMakeLists.txt b/components/sscma_client/CMakeLists.txt
index d620bb7..0fedcf7 100644
--- a/components/sscma_client/CMakeLists.txt
+++ b/components/sscma_client/CMakeLists.txt
@@ -8,7 +8,7 @@ set(srcs "src/sscma_client_ops.c"
          "src/sscma_client_flasher_we2_spi.c"
          )
 set(includes "include" "interface")
-set(require "json" "mbedtls" "esp_timer")
+set(require "json" "mbedtls" "esp_timer" "espressif__esp_io_expander")
 set(priv_requires "driver")
 
 idf_component_register(SRCS ${srcs}
diff --git a/components/sscma_client/include/sscma_client_commands.h b/components/sscma_client/include/sscma_client_commands.h
index 66b3c98..ef62d50 100644
--- a/components/sscma_client/include/sscma_client_commands.h
+++ b/components/sscma_client/include/sscma_client_commands.h
@@ -18,7 +18,7 @@
 #define CMD_PREFIX_LEN (sizeof(CMD_PREFIX) - 1)
 #define CMD_SUFFIX_LEN (sizeof(CMD_SUFFIX) - 1)
 
-#define CMD_WAIT_DELAY 2000 // ms
+#define CMD_WAIT_DELAY (2000 / portTICK_PERIOD_MS) // 2000ms converted to ticks
 
 #define CMD_AT_ID         "ID"
 #define CMD_AT_NAME       "NAME"
diff --git a/components/sscma_client/src/sscma_client_ops.c b/components/sscma_client/src/sscma_client_ops.c
index cb07fdd..c7b457b 100644
--- a/components/sscma_client/src/sscma_client_ops.c
+++ b/components/sscma_client/src/sscma_client_ops.c
@@ -224,9 +224,10 @@ static void sscma_client_process(void *arg)
                 rlen = client->rx_buffer.len - client->rx_buffer.pos;
                 if (rlen <= 0)
                 {
-                    ESP_LOGW(TAG, "rx buffer is full");
-                    client->rx_buffer.pos = 0;
-                    continue;
+                    // Buffer is full — fall through to parse existing data.
+                    // If parsing frees space, next iteration can read more.
+                    // If no complete reply found, discard buffer below.
+                    rlen = 0;
                 }
             }
 
@@ -244,7 +245,8 @@ static void sscma_client_process(void *arg)
             client->rx_buffer.pos = new_pos;
 
 
-            client->rx_buffer.data[client->rx_buffer.pos] = 0;
+            if (client->rx_buffer.pos < client->rx_buffer.len)
+                client->rx_buffer.data[client->rx_buffer.pos] = 0;
             while ((suffix = strnstr(client->rx_buffer.data, RESPONSE_SUFFIX, client->rx_buffer.pos)) != NULL)
             {
                 if ((prefix = strnstr(client->rx_buffer.data, RESPONSE_PREFIX, suffix - client->rx_buffer.data)) != NULL)
@@ -256,9 +258,11 @@ static void sscma_client_process(void *arg)
                         reply.len = len;
                         memcpy(reply.data, prefix, len);
 
-                        // delete this reply from rx buffer
-                        memmove(client->rx_buffer.data, suffix + RESPONSE_SUFFIX_LEN, client->rx_buffer.pos - (suffix - client->rx_buffer.data) - RESPONSE_PREFIX_LEN);
-                        client->rx_buffer.pos -= len;
+                        // delete this reply (and any garbage before prefix) from rx buffer
+                        int bytes_after_reply = client->rx_buffer.pos - (suffix - client->rx_buffer.data + RESPONSE_SUFFIX_LEN);
+                        if (bytes_after_reply < 0) bytes_after_reply = 0;
+                        memmove(client->rx_buffer.data, suffix + RESPONSE_SUFFIX_LEN, bytes_after_reply);
+                        client->rx_buffer.pos = bytes_after_reply;
 
                         reply.data[len] = 0;
                         reply.payload = cJSON_Parse(reply.data);
@@ -421,13 +425,59 @@ static void sscma_client_process(void *arg)
                 }
                 else
                 {
-                    // discard this reply
+                    // discard this reply (no matching prefix found)
                     ESP_LOGW(TAG, "Invalid reply: %d/%d", client->rx_buffer.pos, client->rx_buffer.len);
-                    memmove(client->rx_buffer.data, suffix + RESPONSE_SUFFIX_LEN, client->rx_buffer.pos - (suffix - client->rx_buffer.data) - RESPONSE_PREFIX_LEN);
-                    client->rx_buffer.pos -= suffix - client->rx_buffer.data + RESPONSE_SUFFIX_LEN;
+                    int bytes_after_suffix = client->rx_buffer.pos - (suffix - client->rx_buffer.data + RESPONSE_SUFFIX_LEN);
+                    if (bytes_after_suffix < 0) bytes_after_suffix = 0;
+                    memmove(client->rx_buffer.data, suffix + RESPONSE_SUFFIX_LEN, bytes_after_suffix);
+                    client->rx_buffer.pos = bytes_after_suffix;
                     client->rx_buffer.data[client->rx_buffer.pos] = 0;
                 }
             }
+
+            // Smart discard: if buffer is nearly full with no parseable replies,
+            // keep only data from the last \r{ prefix (potential partial AT reply).
+            if (client->rx_buffer.pos >= client->rx_buffer.len - 256)
+            {
+                char *last_prefix = NULL;
+                for (int i = client->rx_buffer.pos - 2; i >= 0; i--)
+                {
+                    if (client->rx_buffer.data[i] == '\r' && client->rx_buffer.data[i + 1] == '{')
+                    {
+                        last_prefix = &client->rx_buffer.data[i];
+                        break;
+                    }
+                }
+                if (last_prefix && last_prefix > client->rx_buffer.data)
+                {
+                    int keep = client->rx_buffer.pos - (last_prefix - client->rx_buffer.data);
+                    memmove(client->rx_buffer.data, last_prefix, keep);
+                    client->rx_buffer.pos = keep;
+                    ESP_LOGW(TAG, "rx buffer full, kept %d bytes from last prefix", keep);
+                }
+                else
+                {
+                    ESP_LOGW(TAG, "rx buffer full, no prefix found, discarding %d bytes. First 32: %02x %02x %02x %02x %02x %02x %02x %02x %02x %02x %02x %02x %02x %02x %02x %02x %02x %02x %02x %02x %02x %02x %02x %02x %02x %02x %02x %02x %02x %02x %02x %02x",
+                        client->rx_buffer.pos,
+                        (uint8_t)client->rx_buffer.data[0], (uint8_t)client->rx_buffer.data[1],
+                        (uint8_t)client->rx_buffer.data[2], (uint8_t)client->rx_buffer.data[3],
+                        (uint8_t)client->rx_buffer.data[4], (uint8_t)client->rx_buffer.data[5],
+                        (uint8_t)client->rx_buffer.data[6], (uint8_t)client->rx_buffer.data[7],
+                        (uint8_t)client->rx_buffer.data[8], (uint8_t)client->rx_buffer.data[9],
+                        (uint8_t)client->rx_buffer.data[10], (uint8_t)client->rx_buffer.data[11],
+                        (uint8_t)client->rx_buffer.data[12], (uint8_t)client->rx_buffer.data[13],
+                        (uint8_t)client->rx_buffer.data[14], (uint8_t)client->rx_buffer.data[15],
+                        (uint8_t)client->rx_buffer.data[16], (uint8_t)client->rx_buffer.data[17],
+                        (uint8_t)client->rx_buffer.data[18], (uint8_t)client->rx_buffer.data[19],
+                        (uint8_t)client->rx_buffer.data[20], (uint8_t)client->rx_buffer.data[21],
+                        (uint8_t)client->rx_buffer.data[22], (uint8_t)client->rx_buffer.data[23],
+                        (uint8_t)client->rx_buffer.data[24], (uint8_t)client->rx_buffer.data[25],
+                        (uint8_t)client->rx_buffer.data[26], (uint8_t)client->rx_buffer.data[27],
+                        (uint8_t)client->rx_buffer.data[28], (uint8_t)client->rx_buffer.data[29],
+                        (uint8_t)client->rx_buffer.data[30], (uint8_t)client->rx_buffer.data[31]);
+                    client->rx_buffer.pos = 0;
+                }
+            }
         }
     }
 }
